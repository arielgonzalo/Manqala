<%= form_with(model: invoice, local: true) do |form| %>
  <% if invoice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invoice.errors.count, "error") %> prohibited this invoice from being saved:</h2>

      <ul>
      <% invoice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date_time %>
    <%= form.time_select :date_time, id: :invoice_date_time %>
  </div>

  <div class="field">
    <%= form.label :client_id %>
    <%= form.text_field :client_id, id: :invoice_client_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :invoice_amount %>
  </div>

  <div class="field">
    <%= form.label :tax_amount %>
    <%= form.text_field :tax_amount, id: :invoice_tax_amount %>
  </div>

  <div class="field">
    <%= form.label :total_amount %>
    <%= form.text_field :total_amount, id: :invoice_total_amount %>
  </div>

  <div class="field">
    <%= form.label :table_number %>
    <%= form.number_field :table_number, id: :invoice_table_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
