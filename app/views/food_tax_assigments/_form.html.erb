<%= form_with(model: food_tax_assigment, local: true) do |form| %>
  <% if food_tax_assigment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food_tax_assigment.errors.count, "error") %> prohibited this food_tax_assigment from being saved:</h2>

      <ul>
      <% food_tax_assigment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :food_id %>
    <%= form.text_field :food_id, id: :food_tax_assigment_food_id %>
  </div>

  <div class="field">
    <%= form.label :tax_id %>
    <%= form.text_field :tax_id, id: :food_tax_assigment_tax_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
