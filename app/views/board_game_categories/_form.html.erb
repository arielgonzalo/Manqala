<%= form_with(model: board_game_category, local: true) do |form| %>
  <% if board_game_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(board_game_category.errors.count, "error") %> prohibited this board_game_category from being saved:</h2>

      <ul>
      <% board_game_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label "Nombre" %>
    <%= form.text_field :name, class: "form-control", id: :board_game_category_name %>
  </div>

  <div class="form-group">
    <%= form.label "Descripción" %>
    <%= form.text_field :description, class: "form-control", id: :board_game_category_description %>
  </div>

  <div class="actions">

    <%= button_tag(type: 'submit') do %>
      <div style="height: 110px;" class="btn btn-default btn-lg">
            <h1 class="display-1"><span class="glyphicon glyphicon-ok-sign"></span>Guardar</h1>
      </div>
    <% end %>
  </div>
<% end %>
