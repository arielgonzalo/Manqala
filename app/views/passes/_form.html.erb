<%= form_with(model: pass, local: true) do |form| %>
  <% if pass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pass.errors.count, "error") %> prohibited this pass from being saved:</h2>

      <ul>
      <% pass.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start_time %>
    <%= form.date_select :start_time, id: :pass_start_time %>
  </div>

  <div class="field">
    <%= form.label :end_time %>
    <%= form.date_select :end_time, id: :pass_end_time %>
  </div>

  <div class="field">
    <%= form.label :client_id %>
    <%= form.text_field :client_id, id: :pass_client_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
